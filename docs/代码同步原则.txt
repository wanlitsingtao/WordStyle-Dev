代码同步原则：
1、做好工程目录和发布目录前后台程序代码比较，建立完整的同步清单
2、遵循"系统性思考"原则，将修改的前后台所有程序代码同步到发布目录，使用目录级同步，而非文件级同步，同步后必须验证完整性，建立自动化同步脚本
3、如果数据库有改变做好数据库同步，完整对比工作目录和发布目录对应的数据库，生成数据库升级脚本，脚本放到dbscript目录下
4、确保升级后的程序在云端正确运行
Wordstyle项目：
1、维护工作目录和发布目录两套环境配置，满足工作目录测试和发布目录正式运行，在程序同步时能够区分，不能错误覆盖。