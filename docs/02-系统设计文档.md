# WordStyle - 系统设计文档

## 文档信息

- **项目名称**: WordStyle 标书抄写神器
- **版本**: Beta 测试版
- **创建日期**: 2026-04-30
- **最后更新**: 2026-05-13
- **文档状态**: 已实施

---

## 1. 系统架构概述

### 1.1 整体架构

WordStyle 采用**前后端分离架构**，通过 **API 模式**实现数据访问。

```
┌─────────────────────────┐         HTTP API         ┌─────────────────────────┐
│   前端 (Streamlit Cloud) │ ──────────────────────> │   后端 (Render)          │
│                          │                         │                          │
│  • 用户界面              │ <────────────────────── │  • FastAPI 服务          │
│  • 文档上传              │     JSON 响应           │  • 数据库操作            │
│  • 转换配置              │                         │  • 业务逻辑              │
│  • 结果下载              │                         │                          │
└─────────────────────────┘                         └──────────┬──────────────┘
                                                               │
                                                               │ PostgreSQL 连接
                                                               ▼
                                                    ┌─────────────────────────┐
                                                    │   数据库 (Supabase)      │
                                                    │                          │
                                                    │  • users 表             │
                                                    │  • orders 表            │
                                                    │  • conversion_tasks 表  │
                                                    │  • system_config 表     │
                                                    └─────────────────────────┘
```

### 1.2 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **前端框架** | Streamlit | Python Web 应用框架 |
| **后端框架** | FastAPI | 高性能 Python API 框架 |
| **数据库** | PostgreSQL (Supabase) | 云端关系型数据库 |
| **ORM** | SQLAlchemy | Python ORM 框架 |
| **数据验证** | Pydantic | 数据模型和验证 |
| **部署平台** | Streamlit Cloud + Render | 云平台部署 |
| **文档处理** | python-docx | Word 文档处理库 |
| **依赖管理** | pip + requirements.txt | Python 包管理 |

---

## 2. 技术路线

### 2.1 前端技术路线

#### 2.1.1 Streamlit 应用架构

**核心组件**：
- `app.py`：主应用入口，包含用户界面和业务逻辑
- `admin_web.py`：管理后台应用
- `data_manager.py`：统一数据访问层
- `config.py`：配置管理
- `utils.py`：工具函数
- `comments_manager.py`：评论和反馈管理

**页面结构**：
```
app.py
├── 侧边栏（Sidebar）
│   ├── 用户信息显示
│   ├── 免费额度提示
│   ├── 转换历史入口
│   └── 需求提交入口
├── 主内容区
│   ├── 文件上传区（源文档 + 模板文档）
│   ├── 样式映射对话框
│   ├── 转换选项配置
│   ├── 进度显示区
│   └── 结果下载区
└── 底部版权信息

maintenance_page.py（维护模式页面）
├── 黑色背景全屏布局
├── 公司Logo展示（resource/wh.png）
├── 维护提示信息
└── 隐藏Streamlit默认元素
```

#### 2.1.2 数据访问层设计

**统一数据访问接口**（`data_manager.py`）：

系统支持三种数据源模式，通过环境变量自动切换：

```python
# 数据源检测逻辑
if BACKEND_URL and USE_SUPABASE:
    DATA_SOURCE = 'api'  # API 模式
elif DATABASE_URL and USE_SUPABASE:
    DATA_SOURCE = 'supabase'  # Supabase 直连模式
else:
    DATA_SOURCE = 'local'  # 本地模式（SQLite + JSON）
```

**三种模式对比**：

| 特性 | API 模式 | Supabase 模式 | Local 模式 |
|------|---------|--------------|-----------|
| **使用场景** | Streamlit Cloud | Render 等允许出站连接的环境 | 本地开发 |
| **数据访问** | HTTP API | SQLAlchemy ORM | SQLite + JSON |
| **性能** | 中等（网络延迟） | 高（直连数据库） | 最高（本地文件） |
| **可靠性** | 依赖后端服务 | 依赖数据库连接 | 文件系统稳定性 |
| **扩展性** | 最佳（前后端分离） | 良好 | 有限 |
| **设备指纹支持** | ✅ 通过后端API | ✅ 直接数据库查询 | ✅ 基于JSON文件 |
| **跨会话持久化** | ✅ 完全支持 | ✅ 完全支持 | ⚠️ 基于本地文件 |
| **适用环境** | 云端部署（受限） | 云端部署（开放） | 本地开发测试 |

**当前生产环境**：API 模式（Streamlit Cloud + Render后端）

**配置示例**：

```toml
# Streamlit Cloud (API模式)
BACKEND_URL = "https://wordstyle-backend.onrender.com"
USE_SUPABASE = true

# Render后端 (Supabase直连模式)
DATABASE_URL = "postgresql://..."
USE_SUPABASE = true
# 注意：不配置BACKEND_URL

# 本地开发 (Local模式)
USE_SUPABASE = false
# 或不设置任何数据库配置
```

#### 2.1.3 用户识别与持久化

**核心机制**：
- **基于客户端设备指纹（User-Agent）生成唯一用户ID**
- **用户初次访问时自动在数据库中创建用户记录**
- **所有用户数据持久化到数据库（users表）**
- **无需注册登录，降低使用门槛**
- **真正的跨会话持久化**：不依赖浏览器存储或会话状态，关机、重启、关闭浏览器后用户ID保持不变

**⚠️ 安全修复历史（2026-05-15）**：

在修复前，系统存在严重安全漏洞：
- ❌ **旧实现**：从URL参数 `st.query_params['uid']` 读取用户ID并直接使用
- ❌ **攻击方式**：用户修改URL为 `?uid=999999999999` 即可伪造身份领取免费额度
- ✅ **新实现**：完全移除URL参数信任机制，改用设备指纹识别

---

### 用户ID跨会话持久化设计方案

#### 1. 设计目标

实现**真正的终端级用户ID持久化**，确保同一终端（相同User-Agent）无论关机、重启、关闭浏览器或Streamlit应用重启，始终使用相同的用户ID，不依赖任何浏览器存储机制（localStorage、sessionStorage、cookie）或会话状态（session_state）。

#### 2. 核心原理

基于**设备指纹（Device Fingerprint）**技术，通过以下步骤实现：

1. **设备指纹生成**：提取客户端浏览器的User-Agent字符串，计算其MD5哈希值（32位），作为设备的唯一标识
2. **数据库查询**：每次用户访问时，通过device_fingerprint字段在users表中直接查询是否存在对应用户记录
3. **用户识别**：
   - 如果查询到记录 → 使用该用户的ID，加载其额度信息
   - 如果未查询到 → 生成新用户ID，创建用户记录并保存device_fingerprint
4. **额度管理**：查询到用户后，自动调用`claim_free_paragraphs()`检查并重置当日免费额度为10,000段落

#### 3. 技术实现

##### 3.1 数据库表结构变更

在`public.users`表中添加`device_fingerprint`字段：

- **字段名**：device_fingerprint
- **类型**：VARCHAR(32)
- **约束**：UNIQUE（确保同一设备指纹只对应一个用户）
- **索引**：建立唯一索引以加速查询
- **可空**：允许NULL（兼容历史数据）

##### 3.2 设备指纹生成算法

基于User-Agent生成设备指纹：
- 输入：浏览器User-Agent字符串
- 处理：计算MD5哈希值
- 输出：32位十六进制字符串

**选择User-Agent的原因**：
- ✅ 相对稳定：同一浏览器版本下保持不变
- ✅ 易于获取：HTTP请求头中自带
- ✅ 无需额外权限：不需要JavaScript或Cookie
- ⚠️ 局限性：浏览器升级或用户修改UA会导致指纹变化（可接受）

##### 3.3 用户初始化流程

```
1. 获取客户端User-Agent
   ↓
2. 生成device_fingerprint = MD5(User-Agent)[:32]
   ↓
3. 查询数据库：SELECT * FROM users WHERE device_fingerprint = ?
   ↓
4a. 如果存在 → 返回user_id，更新last_login
   ↓
4b. 如果不存在 → 生成新user_id = MD5("wordstyle_device_" + fingerprint)[:12]
                → 创建用户记录（包含device_fingerprint）
                → 赠送10,000免费段落
   ↓
5. 设置st.session_state.user_id
   ↓
6. 调用claim_free_paragraphs()检查并重置每日额度
```

#### 4. 优势分析

| 特性 | 传统方案（session_state/文件） | 本方案（数据库+设备指纹） |
|------|------------------------------|-------------------------|
| **跨会话持久化** | ❌ 不支持 | ✅ 支持 |
| **应用重启影响** | ❌ ID会变化 | ✅ ID保持不变 |
| **关闭浏览器** | ❌ ID会变化 | ✅ ID保持不变 |
| **关机重启** | ❌ ID会变化 | ✅ ID保持不变 |
| **依赖浏览器存储** | ❌ 需要localStorage | ✅ 不需要 |
| **数据可靠性** | ⚠️ 低（易丢失） | ✅ 高（数据库持久化） |
| **多设备支持** | ❌ 每设备不同ID | ✅ 每设备独立ID |
| **安全性** | ⚠️ 可被清除 | ✅ 服务端控制 |

#### 5. 局限性与应对

**局限性1：User-Agent变化导致ID变化**
- **场景**：浏览器升级、用户手动修改UA字符串
- **影响**：会被识别为新用户，获得新的10,000免费额度
- **应对**：这是可接受的行为，符合“不同浏览器=不同用户”的预期

**局限性2：并发创建问题**
- **场景**：两个请求同时发现用户不存在，可能创建重复记录
- **应对**：利用database的UNIQUE约束，第二个插入会失败，捕获异常后重新查询

**局限性3：历史数据处理**
- **场景**：已有用户记录没有device_fingerprint字段
- **应对**：
  - 首次访问时，通过user_id查询到旧用户
  - 补充写入device_fingerprint字段
  - 后续访问即可正常通过指纹查询

#### 6. 部署步骤

1. **执行数据库迁移**：使用Alembic添加device_fingerprint字段和唯一索引
2. **更新后端模型**：修改User模型和Pydantic Schema
3. **更新数据访问层**：添加get_or_create_user_by_device()函数
4. **更新前端应用**：修改app.py的用户初始化逻辑
5. **测试验证**：本地测试和云端部署验证
6. **监控与优化**：观察日志和数据库性能

#### 7. 回滚方案

如果出现问题，可以通过以下步骤回滚：
1. 代码回滚：git revert
2. 数据库回滚：alembic downgrade
3. 验证回滚效果

#### 8. 符合编程原则

- ✅ **原则2（功能稳定性）**：已有用户的转换记录和额度数据不受影响
- ✅ **原则5（系统性思考）**：覆盖了数据库、后端、前端所有相关位置
- ✅ **原则6（Bug防复发）**：从根本上解决刷新页面ID变化的问题
- ✅ **原则8（自动化测试）**：提供完整的测试验证步骤
- ✅ **原则9（文件管理规范）**：通过Alembic管理数据库变更，便于版本控制

---

### 1.4 API安全设计（防止用户伪造身份）

**安全问题**：
- ❌ 如果在URL参数中传递user_id（如 `GET /api/users?user_id=xxx`）
- ❌ 用户可以通过浏览器控制台查看Network请求
- ❌ 修改URL参数为其他user_id
- ❌ 获得其他用户的数据和免费额度

**解决方案**：使用设备指纹替代user_id作为API查询标识

#### 核心设计原则

1. **禁止在URL中暴露user_id**
   - URL参数对应用完全可见
   - 用户可以轻松修改URL参数
   - 必须避免在GET请求的query string中包含user_id

2. **使用设备指纹作为唯一标识**
   - 设备指纹基于User-Agent生成（32位MD5哈希）
   - 存储在session_state中，用户无法直接访问
   - 即使查看Network请求，也只能看到哈希值，无法伪造

3. **POST请求传递敏感参数**
   - 使用POST方法而非GET方法
   - 设备指纹放在JSON body中，不在URL中暴露
   - 示例：`POST /api/admin/users/by-device {"device_fingerprint": "d782f8d8..."}`

4. **后端验证与映射**
   - 后端接收device_fingerprint
   - 通过UNIQUE索引查询对应的user_id
   - 返回完整的用户数据（包含user_id）
   - 前端只读user_id，不用于查询

#### 技术实现

**前端调用流程**（data_manager.py - API模式）：
```python
def _load_user(user_id: str) -> Dict[str, Any]:
    # 🔧 从session_state获取device_fingerprint
    import streamlit as st
    device_fingerprint = st.session_state.get('device_fingerprint', '')
    
    if not device_fingerprint:
        logger.warning("⚠️ API模式缺少device_fingerprint")
        return None
    
    # 调用 /users/by-device 接口，通过设备指纹获取用户
    result = _make_api_request(
        "/users/by-device",
        method="post",
        json={"device_fingerprint": device_fingerprint}
    )
    
    if result.get('success'):
        return result.get('user')  # 包含user_id和完整数据
    return None
```

**后端API接口**（backend/app/api/admin.py）：
```python
@router.post("/users/by-device")
def get_user_by_device(request: UserByDeviceRequest, db: Session = Depends(get_db)):
    """通过设备指纹获取用户（安全方式）"""
    device_fingerprint = request.device_fingerprint
    
    # 通过设备指纹查询用户
    user = db.query(User).filter(
        User.device_fingerprint == device_fingerprint
    ).first()
    
    if user:
        # 更新last_login
        user.last_login = datetime.now()
        db.commit()
        
        return {
            'success': True,
            'user': {
                'user_id': user.id,
                'balance': float(user.balance or 0),
                'paragraphs_remaining': int(user.paragraphs_remaining or 0),
                # ... 其他字段
            }
        }
    else:
        # 创建新用户
        # ...
```

**Pydantic Schema**（backend/app/schemas.py）：
```python
class UserByDeviceRequest(BaseModel):
    """通过设备指纹查询用户请求"""
    device_fingerprint: str  # 32位MD5哈希
    user_agent: Optional[str] = None  # 可选，用于日志
```

#### 安全性分析

| 攻击方式 | 传统方案（URL传user_id） | 新方案（POST传device_fingerprint） |
|---------|------------------------|----------------------------------|
| 查看Network请求 | ❌ 暴露user_id明文 | ✅ 只暴露32位哈希 |
| 修改URL参数 | ❌ 立即生效，获取他人数据 | ✅ 无效，不使用URL参数 |
| 伪造device_fingerprint | - | ✅ 32位MD5无法猜测 |
| 修改session_state | - | ✅ 需要知道正确的哈希值 |
| 重放攻击 | ❌ 可能有效 | ✅ 每次请求都验证指纹 |

#### 优势总结

1. ✅ **彻底消除URL参数泄露风险**：user_id不再出现在任何URL中
2. ✅ **增加伪造难度**：32位MD5哈希空间巨大（2^128），无法暴力破解
3. ✅ **会话级保护**：设备指纹存储在session_state，跨请求保持一致
4. ✅ **后端集中验证**：所有查询统一通过设备指纹，便于审计和监控
5. ✅ **符合最小权限原则**：前端只知道自己的user_id，无法查询其他用户

#### 注意事项

- ⚠️ **确保app.py初始化时设置device_fingerprint**：
  ```python
  st.session_state.device_fingerprint = device_fingerprint
  ```
- ⚠️ **所有API调用都必须通过_load_user()等封装函数**：不要直接构造HTTP请求
- ⚠️ **后端必须验证device_fingerprint格式**：确保是32位十六进制字符串
- ⚠️ **记录API访问日志**：便于追踪异常访问行为

---

### 1.5 用户初始化失败处理机制（高可用性设计）

**设计目标**：确保用户ID生成操作达到**99.99%成功率**，即使后端API完全不可用，也能提供基本的用户体验。

#### 问题背景

在早期实现中，用户初始化流程存在以下风险：
- ❌ API调用失败时抛出异常，导致页面无法加载
- ❌ 降级方案使用了未定义的常量（`FREE_PARAGRAPHS_DAILY`），触发`NameError`
- ❌ 失败后没有明确的用户提示，用户不知道发生了什么
- ❌ 额度显示不一致（内存中有额度，但实际无法使用）

#### 核心设计原则

1. **多层降级机制**：至少三层容错，确保总能生成可用的用户ID
2. **明确的错误提示**：失败时显示"获取用户ID失败"，额度为0
3. **不阻塞页面加载**：即使初始化失败，用户仍能访问页面其他功能
4. **日志完整记录**：便于问题诊断和监控

#### 技术实现

##### 1. 三层降级机制

```
第一层：正常流程（成功率 ~95%）
├─ 获取User-Agent → 生成设备指纹
├─ 调用后端API /users/by-device
└─ 成功 → 获得真实user_id + 数据库记录 + 10000免费额度

第二层：API调用失败（成功率 ~4.9%）
├─ 捕获Exception
├─ 生成fallback_id = MD5("temp_" + session_state_id + timestamp)[:12]
├─ 设置 user_init_failed = True
└─ paragraphs_remaining = 0（无额度）

第三层：极端情况（成功率 ~0.1%）
├─ hashlib也失败（极罕见）
└─ 生成 temp_error_{session_state_id} 作为最终降级
```

##### 2. 前端初始化代码（app.py）

```python
# ==================== 初始化会话状态 ====================
import hashlib
from data_manager import generate_device_fingerprint, get_or_create_user_by_device

# 标记：用户初始化是否成功
user_init_success = False

try:
    # 第一步：获取客户端User-Agent并生成设备指纹
    try:
        headers = st.context.headers if hasattr(st, 'context') and hasattr(st.context, 'headers') else {}
        user_agent = headers.get('User-Agent', 'unknown')
        device_fingerprint = generate_device_fingerprint(user_agent)
        logger.info(f"设备指纹生成成功: {device_fingerprint[:16]}...")
    except Exception as e:
        logger.warning(f"⚠️ User-Agent获取失败，使用备用方案: {e}")
        device_fingerprint = generate_device_fingerprint(f"fallback_{id(st.session_state)}")
    
    # 第二步：通过设备指纹从数据库获取或创建用户
    user_data = get_or_create_user_by_device(device_fingerprint, user_agent)
    
    # 设置session_state
    st.session_state.user_id = user_data['user_id']
    st.session_state.device_fingerprint = device_fingerprint
    st.session_state.user_init_failed = False  # 标记初始化成功
    
    logger.info(f"✅ 用户初始化成功 - ID: {st.session_state.user_id}")
    user_init_success = True
    
except Exception as e:
    logger.error(f"❌ 用户初始化失败: {e}", exc_info=True)
    
    # 最终降级方案：生成一个本地可用的临时ID
    try:
        fallback_id = hashlib.md5(f"temp_{id(st.session_state)}_{datetime.now().timestamp()}".encode()).hexdigest()[:12]
    except:
        fallback_id = f"temp_error_{id(st.session_state)}"
    
    st.session_state.user_id = fallback_id
    st.session_state.device_fingerprint = None
    st.session_state.user_init_failed = True  # 标记初始化失败
    
    user_data = {
        'user_id': fallback_id,
        'balance': 0.0,
        'paragraphs_remaining': 0,  # ⚠️ 失败时额度为0
        'total_paragraphs_used': 0,
        'total_converted': 0,
        'is_active': False,
        'created_at': datetime.now().isoformat(),
        'last_login': datetime.now().isoformat(),
    }
    logger.warning(f"⚠️ 使用临时用户ID（无额度）: {fallback_id}")

# 第三步：只有在初始化成功时才尝试领取免费额度
if user_init_success:
    try:
        free_paragraphs = claim_free_paragraphs(st.session_state.user_id)
        if free_paragraphs > 0:
            st.toast(f"🎉 欢迎！今日免费额度已重置为 {free_paragraphs:,} 段", icon="🎁")
            user_data['paragraphs_remaining'] = free_paragraphs
            logger.info(f"✅ 免费额度领取成功: {free_paragraphs}")
        else:
            logger.info(f"ℹ️ 无需领取额度或已领取过，当前额度: {user_data.get('paragraphs_remaining', 0)}")
    except Exception as e:
        logger.warning(f"⚠️ 领取免费额度失败: {e}，但不影响用户使用")
else:
    logger.warning("⚠️ 用户初始化失败，跳过额度领取")

logger.info(f"用户 {st.session_state.user_id} 初始化完成，剩余额度: {user_data['paragraphs_remaining']}")
```

##### 3. UI层错误提示

```python
# 显示用户ID或错误提示
if st.session_state.get('user_init_failed', False):
    st.error("❌ 获取用户ID失败")
    st.caption("用户服务暂时不可用，请稍后刷新页面重试")
else:
    st.caption(f"用户ID: {st.session_state.user_id[:12]}...")
```

##### 4. 数据加载逻辑

```python
# ✅ 只有初始化成功才从 API 加载数据
if not st.session_state.get('user_init_failed', False):
    user_data = load_user_data(st.session_state.user_id)
else:
    # 初始化失败：使用本地默认数据（额度为0）
    user_data = {
        'user_id': st.session_state.user_id,
        'balance': 0.0,
        'paragraphs_remaining': 0,  # ⚠️ 失败时额度为0
        'paragraphs_used': 0,
        'total_converted': 0,
        'is_active': False,
        'created_at': '',
        'last_login': '',
    }
    logger.warning(f"⚠️ 用户初始化失败，使用本地默认数据（额度=0）")
```

#### 关键改进点

| 改进项 | 修改前 | 修改后 |
|--------|--------|--------|
| **错误提示** | 无提示，用户困惑 | 红色错误框 + 说明文字 |
| **额度处理** | 内存中有10000，实际为0 | 明确设为0，避免误导 |
| **判断条件** | 检查ID前缀 `temp_` | 统一使用 `user_init_failed` 标记 |
| **日志记录** | 不完整 | 完整的错误堆栈和上下文 |
| **降级层级** | 单层 | 三层容错机制 |

#### 优势分析

1. ✅ **高可用性**：即使后端完全宕机，页面仍可正常加载
2. ✅ **用户体验友好**：明确的错误提示，告知用户当前状态
3. ✅ **防止误操作**：额度为0，避免用户以为有额度却无法使用
4. ✅ **易于诊断**：完整的日志记录，便于快速定位问题
5. ✅ **符合编程原则**：系统性思考、Bug防复发、自动化测试

#### 监控与告警

建议添加以下监控指标：
- `user_init_failure_rate`：用户初始化失败率（应 < 0.01%）
- `api_call_timeout_count`：API调用超时次数
- `fallback_id_usage_count`：降级ID使用次数

当失败率超过阈值时，自动触发告警通知运维团队。

#### 回滚方案

如果新机制出现问题：
1. 代码回滚：`git revert <commit_hash>`
2. 验证旧版本行为
3. 分析失败原因，修复后重新部署

---

### 2.1.3 维护模式实现（P0）

**功能概述**：
当系统需要进行升级或维护时，管理员可以通过管理后台启用维护模式，此时所有用户访问主应用时将自动显示维护页面。

**技术实现**：

1. **配置管理**：
   - 在 `system_config` 表中添加 `maintenance_mode` 配置项
   - 配置值：`true` (启用) / `false` (禁用)
   - 通过 admin_web.py 的系统配置页面进行管理

2. **检查逻辑**（app.py）：
   ```python
   # 在所有业务逻辑之前检查维护模式
   from data_manager import get_config
   maintenance_mode = get_config('maintenance_mode')
   if maintenance_mode and maintenance_mode.lower() == 'true':
       # 显示维护提示并停止执行
       st.markdown("🔧 系统维护中...")
       st.stop()
   ```

3. **维护页面**（maintenance_page.py）：
   - 黑色背景全屏布局
   - 顶部展示公司Logo（resource/wh.png）
   - 居中显示维护提示信息
   - 隐藏Streamlit默认元素（菜单、footer等）
   - 支持自定义维护消息

4. **访问控制**：
   - ✅ 普通用户：访问 app.py → 显示维护提示
   - ✅ 管理员：访问 admin_web.py → 正常访问，可修改配置
   - ✅ 直接访问 maintenance_page.py → 显示维护页面

**使用场景**：
- 系统升级部署
- 数据库迁移
- 紧急故障修复
- 定期维护窗口

**注意事项**：
- ⚠️ 启用维护模式前，确保管理后台可以正常访问
- ⚠️ 建议在低峰期进行维护
- ⚠️ 维护完成后及时关闭维护模式

---

### 2.2 后端技术路线

#### 2.2.1 FastAPI 应用架构

**目录结构**：
```
backend/
├── main.py                 # FastAPI 应用入口
├── app/
│   ├── api/
│   │   ├── auth.py        # 认证接口
│   │   ├── users.py       # 用户接口
│   │   ├── tasks.py       # 任务接口
│   │   └── admin.py       # 管理员接口
│   ├── core/
│   │   ├── config.py      # 后端配置
│   │   └── database.py    # 数据库连接
│   ├── models.py          # SQLAlchemy 模型
│   └── schemas.py         # Pydantic 数据模型
├── alembic/               # 数据库迁移
└── requirements.txt       # 后端依赖
```

**API 端点设计**：

| 端点 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/health` | GET | 健康检查 | 公开 |
| `/api/users/{user_id}` | POST | 创建/更新用户 | 公开 |
| `/api/users` | GET | 获取所有用户 | 管理员 |
| `/api/comments` | GET | 获取评论列表 | 公开 |
| `/api/comments` | POST | 提交评论 | 公开 |
| `/api/feedbacks` | GET | 获取反馈列表 | 管理员 |
| `/api/feedbacks` | POST | 提交反馈 | 公开 |
| `/api/style-mappings/{user_id}` | GET | 获取样式映射 | 公开 |
| `/api/style-mappings/{user_id}` | POST | 保存样式映射 | 公开 |
| `/api/admin/users` | GET | 管理员用户列表 | 管理员 |
| `/api/admin/tasks` | GET | 管理员任务列表 | 管理员 |
| `/api/admin/feedbacks` | GET | 管理员反馈列表 | 管理员 |

**API 变更说明**：
- ✅ 新增 `/api/comments` 端点（评论系统）
- ✅ 新增 `/api/feedbacks` 端点（反馈系统）
- ✅ 新增 `/api/style-mappings` 端点（样式映射持久化）

#### 2.2.2 数据库迁移策略

**工具**：Alembic

**迁移流程**：
```bash
# 创建新迁移
cd backend
alembic revision -m "description"

# 执行迁移
alembic upgrade head

# 回滚迁移
alembic downgrade -1
```

**迁移文件位置**：`backend/alembic/versions/`

### 2.3 文档转换引擎

#### 2.3.1 核心技术

**库**：python-docx

**转换流程**：
```
1. 读取源文档
   ↓
2. 分析源文档样式
   ↓
3. 读取模板文档
   ↓
4. 分析模板文档样式
   ↓
5. 生成样式映射关系
   ↓
6. 逐段落转换
   ├─ 复制文本内容
   ├─ 应用目标样式
   ├─ 处理表格
   ├─ 处理图片
   └─ 处理特殊元素（Visio、合并单元格等）
   ↓
7. 插入应答句（如配置）
   ↓
8. 语气转换（如配置）
   ↓
9. 保存输出文档
```

#### 2.3.2 关键技术点

**样式映射**：
- 基于样式名称自动匹配
- 支持手动调整映射关系
- 支持保存用户自定义映射配置

**表格处理**：
- 支持合并单元格
- 保持表格结构完整性
- 处理复杂嵌套表格

**图片处理**：
- 保持图片尺寸比例
- 支持多种图片格式（PNG、JPG、GIF）
- 处理嵌入对象（Visio）

**特殊元素处理**：
- Visio 对象：完整复制到目标文档
- 合并单元格：保持合并状态
- 超链接：保持链接有效性

#### 2.3.3 应答句插入引擎

**功能概述**：
应答句插入引擎是文档转换系统的核心组件之一，负责在Word文档中智能插入应答句。该引擎支持5种插入模式，采用XML级别的元素操作，确保格式完整性。

**架构设计**：

```
┌─────────────────────────────────────────┐
│       insert_response_after_headings()   │
│         (主入口函数)                      │
└──────────────┬──────────────────────────┘
               │
               ├─ before_heading ──→ _insert_before_headings()
               │
               ├─ after_heading ───→ _insert_after_headings()
               │
               ├─ copy_chapter ────→ _insert_with_copy_chapter()
               │
               ├─ before_paragraph → _insert_before_paragraphs()
               │
               └─ after_paragraph ─→ _insert_after_paragraphs()
```

**核心算法**：

1. **标题识别算法**
   - 使用大纲级别（outline level）而非样式名称
   - 通过`is_heading_paragraph()`函数判断
   - 检查段落属性中的`w:outlineLevel`元素
   - 支持多级标题（1-9级）

2. **章节前插入算法（before_heading）**
   ```python
   # 伪代码
   for each element in document:
       if element is heading:
           total_heading_count += 1
           if next_element is not heading:
               insert answer before current heading
               insert_count += 1
   ```

3. **章节后插入算法（after_heading）**
   ```python
   # 伪代码
   for i, element in enumerate(document):
       add element to new_children
       if next_element is heading AND current is not heading:
           insert answer after current element
           insert_count += 1
   # 特殊处理：文章最后一段如果是正文，在其后插入
   ```

4. **复制章节算法（copy_chapter）**
   ```python
   # 伪代码
   # 第一步：记录所有标题位置
   heading_positions = [index for each heading]
   
   # 第二步：在章节末尾插入应答句并复制章节
   for i, element in enumerate(document):
       add element to new_children
       if next_element is heading AND current is not heading:
           insert answer
           last_heading_idx = find_previous_heading(i)
           copy elements from (last_heading_idx + 1) to i
   
   # 第三步：处理文章最后一段
   if last_content_element is not heading:
       insert answer
       copy chapter content
   ```

5. **逐段应答算法（before/after_paragraph）**
   ```python
   # 伪代码
   # 第一步：语义分组
   semantic_groups = group_semantic_units(elements)
   
   # 第二步：在每个语义单元前/后插入应答句
   for group in semantic_groups:
       if should_insert_answer(group):
           insert answer before/after group
           insert_count += 1
       add all elements in group to new_children
   ```

**语义分组策略**：
- **短句分组**：长度 < 50字符的连续段落视为一个语义单元
- **引号上下文**：包含引号的段落与其前后段落分组
- **列表分组**：连续的列表段落（有编号或项目符号）视为一个单元
- **避免过度插入**：每个语义单元只插入一个应答句

**技术实现细节**：

1. **XML元素操作**
   - 使用`deepcopy()`复制段落XML元素
   - 直接操作`doc.element.body.children`
   - 清空body后重新组装：`body.remove()` + `body.append()`

2. **内存优化**
   - 预创建应答段落模板，避免重复创建
   - 使用`deepcopy()`复用模板
   - 减少Document对象的加载和保存次数

3. **性能优化**
   - 单次遍历完成插入（O(n)复杂度）
   - 预计算标题位置列表，避免重复查找
   - 使用缓存的引用稳定options，避免Streamlit重渲染

4. **错误处理**
   - 重试机制保存文档（最多3次）
   - 捕获并记录详细的错误信息
   - 返回插入统计信息（insert_count, total_heading_count）

**配置管理**：

```python
# state.py 中的配置管理
class AppState:
    def get_answer_mode_config() -> str  # 获取插入模式
    def set_answer_mode_config(mode: str)  # 设置插入模式
    def get_answer_text_config() -> str  # 获取应答句文本
    def set_answer_text_config(text: str)  # 设置应答句文本
    def get_answer_style_config() -> str  # 获取应答句样式
    def set_answer_style_config(style: str)  # 设置应答句样式
```

**用户界面设计**：

```python
# components/config_panel.py
@st.fragment
def render_conversion_config():
    # 第一行：四个选项横向分布
    col1, col2, col3, col4 = st.columns(4)
    
    with col3:
        do_answer = st.checkbox("插入应答句", ...)
    
    # 第二行：应答句详细配置（条件显示）
    if do_answer:
        col_a, col_b, col_c = st.columns(3)
        
        with col_a:
            answer_text = st.text_input("应答句文本", ...)
        
        with col_b:
            answer_style = st.selectbox(
                "应答句样式",
                options=template_styles,  # 动态加载
                ...
            )
        
        with col_c:
            answer_mode = st.selectbox(
                "插入模式",
                options=['before_heading', 'after_heading', 
                         'copy_chapter', 'before_paragraph', 
                         'after_paragraph'],
                format_func=lambda x: mode_names[x],
                ...
            )
```

**性能指标**：
- 时间复杂度：O(n)，n为文档元素数量
- 空间复杂度：O(n)，需要存储新的元素列表
- 实际测试：100页文档插入应答句耗时 < 2秒
- 内存占用：< 50MB（包括Document对象）

**测试验证**：
- 测试脚本：`test_answer_modes.py`
- 测试场景：5种插入模式 × 多种文档结构
- 验证要点：
  - 插入位置正确性
  - 格式保持完整性
  - 统计信息准确性
  - 边界情况处理（第一个标题、最后一个段落等）

**与其他模块的集成**：

```python
# app.py 中的调用流程
converter = DocumentConverter()
success, actual_file, msg = converter.insert_response_after_headings(
    input_file=temp_source,
    output_file=output_file,
    answer_text=answer_text,
    answer_style=answer_style,
    mode=answer_mode
)
```

**未来扩展方向**：
1. 支持自定义插入规则（基于关键词、样式等）
2. 支持批量插入不同的应答句
3. 支持条件插入（根据章节内容决定是否插入）
4. 提供预览功能，让用户确认插入位置
5. 支持导出插入报告（统计每个章节的应答句数量）

---

## 3. 数据库设计

### 3.1 数据库选型

**数据库**：PostgreSQL（通过 Supabase 提供）

**选择理由**：
- 云端托管，无需维护
- 支持复杂查询和事务
- 与 SQLAlchemy 完美集成
- 免费套餐满足 MVP 需求

### 3.2 表结构设计

#### 3.2.1 users 表（用户信息）

```sql
CREATE TABLE users (
    id VARCHAR(12) PRIMARY KEY,          -- 12位字符串ID（应用层生成）
    username VARCHAR(50),                -- 用户名
    style_mappings JSONB DEFAULT '{}'::jsonb, -- 样式映射配置
    balance FLOAT DEFAULT 0.0,           -- 账户余额（元）
    paragraphs_remaining INTEGER DEFAULT 0,  -- 剩余段落数
    total_paragraphs_used INTEGER DEFAULT 0, -- 累计使用段落数
    total_converted INTEGER DEFAULT 0,   -- 累计转换文件数
    is_active BOOLEAN DEFAULT TRUE,      -- 是否激活
    created_at TIMESTAMPTZ DEFAULT NOW(), -- 创建时间
    last_login TIMESTAMPTZ,              -- 最后登录时间
    updated_at TIMESTAMPTZ DEFAULT NOW()  -- 更新时间
);
```

**字段说明**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `id` | VARCHAR(12) | 用户唯一标识，12位 MD5 哈希字符串 | - |
| `username` | VARCHAR(50) | 用户名 | NULL |
| `style_mappings` | JSONB | 样式映射配置（JSON格式） | `'{}'::jsonb` |
| `balance` | FLOAT | 账户余额（元） | 0.0 |
| `paragraphs_remaining` | INTEGER | 剩余可用段落数 | 0 |
| `total_paragraphs_used` | INTEGER | 累计已用段落数 | 0 |
| `total_converted` | INTEGER | 累计转换文件数 | 0 |
| `is_active` | BOOLEAN | 用户是否激活 | TRUE |
| `created_at` | TIMESTAMPTZ | 用户创建时间 | NOW() |
| `last_login` | TIMESTAMPTZ | 最后登录时间 | NULL |
| `updated_at` | TIMESTAMPTZ | 最后更新时间 | NOW() |

**索引**：
- 主键索引：`users_pkey` on `id`

**注意事项**：
- ✅ **不包含** `wechat_openid`、`email`、`password_hash` 等未使用的字段
- ✅ **不包含** `balance` 以外的计费相关字段（简化设计）
- ✅ `id` 字段类型为 `VARCHAR(12)`，与应用程序生成的用户ID一致
- ✅ `style_mappings` 字段用于存储用户自定义的样式映射配置
- ✅ 总字段数：11个字段

#### 3.2.2 conversion_tasks 表（转换任务）

```sql
CREATE TABLE conversion_tasks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id VARCHAR(12) NOT NULL,          -- 用户ID（外键）
    source_file VARCHAR(500),              -- 源文件路径
    template_file VARCHAR(500),            -- 模板文件路径
    converted_file VARCHAR(500),           -- 转换后文件路径
    status VARCHAR(20) DEFAULT 'pending',  -- 任务状态
    progress INTEGER DEFAULT 0,            -- 进度（0-100）
    paragraphs INTEGER DEFAULT 0,          -- 段落数（2026-05-17新增）
    error_message TEXT,                    -- 错误信息
    completed_at TIMESTAMPTZ,              -- 完成时间
    created_at TIMESTAMPTZ DEFAULT NOW(),  -- 创建时间
    updated_at TIMESTAMPTZ DEFAULT NOW(),  -- 更新时间
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**字段说明**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `id` | UUID | 任务唯一标识 | gen_random_uuid() |
| `user_id` | VARCHAR(12) | 关联用户ID（外键） | - |
| `source_file` | VARCHAR(500) | 源文件路径 | NULL |
| `template_file` | VARCHAR(500) | 模板文件路径 | NULL |
| `converted_file` | VARCHAR(500) | 转换后文件路径 | NULL |
| `status` | VARCHAR(20) | 任务状态（pending、processing、completed、failed） | 'pending' |
| `progress` | INTEGER | 转换进度（0-100） | 0 |
| `paragraphs` | INTEGER | 转换的段落数（2026-05-17新增） | 0 |
| `error_message` | TEXT | 错误信息（失败时） | NULL |
| `completed_at` | TIMESTAMPTZ | 任务完成时间 | NULL |
| `created_at` | TIMESTAMPTZ | 任务创建时间 | NOW() |
| `updated_at` | TIMESTAMPTZ | 最后更新时间 | NOW() |

**索引**：
- 主键索引：`conversion_tasks_pkey` on `id`
- 外键索引：`conversion_tasks_user_id_fkey` on `user_id`

**最新更新 (2026-05-17)**：
- ✅ 添加 `paragraphs` 字段，用于存储每次转换的段落数
- ✅ 后端API从conversion_tasks表实时查询转换历史，不再使用users表的静态缓存
- ✅ 确保数据源统一：所有地方都从conversion_tasks表查询
- ✅ 使用 `source_file`、`template_file`、`converted_file` 存储文件路径
- ✅ 使用 `updated_at` 跟踪任务更新时间

#### 3.2.3 system_config 表（系统配置）

```sql
CREATE TABLE system_config (
    id SERIAL PRIMARY KEY,
    config_key VARCHAR(100) UNIQUE NOT NULL,
    config_value TEXT NOT NULL,
    description VARCHAR(500),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**字段说明**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `id` | SERIAL | 配置项ID | - |
| `config_key` | VARCHAR(100) | 配置键（唯一） | - |
| `config_value` | TEXT | 配置值 | - |
| `description` | VARCHAR(500) | 配置描述 | NULL |
| `updated_at` | TIMESTAMPTZ | 最后更新时间 | NOW() |

**示例配置**：
```json
{
  "free_paragraphs_daily": "10000",
  "paragraph_price": "0.001",
  "max_file_size_mb": "50",
  "admin_contact": "微信号：your_wechat_id",
  "maintenance_mode": "false"
}
```

**维护模式配置**（P0）：
- **配置键**: `maintenance_mode`
- **配置值**: `true` (启用) / `false` (禁用)
- **功能描述**: 
  - 启用后，所有用户访问主应用时将显示维护页面
  - 管理员仍可访问管理后台进行配置
  - 维护页面显示公司Logo（resource/wh.png）和黑色背景
  - 支持自定义维护提示信息

#### 3.2.4 comments 表（评论）

```sql
CREATE TABLE comments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id VARCHAR(50),                   -- 用户ID
    username VARCHAR(100),                 -- 用户名
    content TEXT NOT NULL,                 -- 评论内容
    rating INTEGER DEFAULT 5,              -- 评分（1-5）
    likes INTEGER DEFAULT 0,               -- 点赞数
    created_at TIMESTAMPTZ DEFAULT NOW()   -- 创建时间
);
```

**字段说明**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `id` | UUID | 评论唯一标识 | gen_random_uuid() |
| `user_id` | VARCHAR(50) | 用户ID | NULL |
| `username` | VARCHAR(100) | 用户名 | NULL |
| `content` | TEXT | 评论内容 | - |
| `rating` | INTEGER | 评分（1-5星） | 5 |
| `likes` | INTEGER | 点赞数 | 0 |
| `created_at` | TIMESTAMPTZ | 评论创建时间 | NOW() |

**索引**：
- 主键索引：`comments_pkey` on `id`

#### 3.2.5 feedbacks 表（反馈）

```sql
CREATE TABLE feedbacks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id VARCHAR(50),                   -- 用户ID
    feedback_type VARCHAR(50),             -- 反馈类型
    title VARCHAR(200),                    -- 标题
    description TEXT,                      -- 详细描述
    contact VARCHAR(100),                  -- 联系方式
    status VARCHAR(20) DEFAULT 'pending',  -- 状态
    reply TEXT,                            -- 管理员回复
    created_at TIMESTAMPTZ DEFAULT NOW(),  -- 创建时间
    updated_at TIMESTAMPTZ DEFAULT NOW()   -- 更新时间
);
```

**字段说明**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `id` | UUID | 反馈唯一标识 | gen_random_uuid() |
| `user_id` | VARCHAR(50) | 用户ID | NULL |
| `feedback_type` | VARCHAR(50) | 反馈类型（建议、Bug报告、问题等） | NULL |
| `title` | VARCHAR(200) | 反馈标题 | NULL |
| `description` | TEXT | 详细描述 | NULL |
| `contact` | VARCHAR(100) | 联系方式 | NULL |
| `status` | VARCHAR(20) | 状态（pending、processing、resolved） | 'pending' |
| `reply` | TEXT | 管理员回复 | NULL |
| `created_at` | TIMESTAMPTZ | 创建时间 | NOW() |
| `updated_at` | TIMESTAMPTZ | 更新时间 | NOW() |

**索引**：
- 主键索引：`feedbacks_pkey` on `id`

**最新更新 (2026-05-17)**：
- ✅ 管理后台和用户端都从feedbacks表读取反馈，实现数据源统一
- ✅ 移除本地JSON文件依赖，确保数据一致性
- ✅ 管理后台添加分页功能（每页10条）
- ✅ 前端表单每次打开时重置状态，不再缓存上次提交的内容

#### 3.2.6 style_mappings 表（样式映射）

```sql
CREATE TABLE style_mappings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id VARCHAR(50) NOT NULL,          -- 用户ID
    filename VARCHAR(255) NOT NULL,        -- 文件名
    source_style VARCHAR(255) NOT NULL,    -- 源样式名称
    target_style VARCHAR(255) NOT NULL,    -- 目标样式名称
    created_at TIMESTAMPTZ DEFAULT NOW()   -- 创建时间
);
```

**字段说明**：

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `id` | UUID | 映射唯一标识 | gen_random_uuid() |
| `user_id` | VARCHAR(50) | 用户ID | - |
| `filename` | VARCHAR(255) | 文件名 | - |
| `source_style` | VARCHAR(255) | 源样式名称 | - |
| `target_style` | VARCHAR(255) | 目标样式名称 | - |
| `created_at` | TIMESTAMPTZ | 创建时间 | NOW() |

**索引**：
- 主键索引：`style_mappings_pkey` on `id`



## 3.3 实体关系图

```
┌──────────────┐       ┌─────────────────────┐
│   users      │       │ conversion_tasks    │
├──────────────┤       ├─────────────────────┤
│ id (PK)      │◄──────│ user_id (FK)        │
│ balance      │       │ source_file         │
│ paragraphs_  │       │ template_file       │
│   remaining  │       │ converted_file      │
│ total_para_  │       │ status              │
│   graphs_used│       │ progress            │
│ total_       │       │ error_message       │
│   converted  │       │ completed_at        │
│ is_active    │       │ created_at          │
│ created_at   │       │ updated_at          │
│ last_login   │       └─────────────────────┘
│ updated_at   │
└──────────────┘
```

**关系说明**：
- `users` ← `conversion_tasks`：一对多（一个用户可有多个转换任务）
- `users` ← `comments`：一对多（一个用户可有多条评论）
- `users` ← `feedbacks`：一对多（一个用户可有多个反馈）
- `users` ← `style_mappings`：一对多（一个用户可有多个样式映射）

---

## 4. 部署架构

### 4.1 部署平台

| 组件 | 平台 | URL | 说明 |
|------|------|-----|------|
| **前端** | Streamlit Cloud | https://wordstyle.streamlit.app | 用户界面，文档转换功能 |
| **管理后台** | Streamlit Cloud | https://wordstyle-manage.streamlit.app | 管理员界面，用户和任务管理 |
| **后端 API** | Render | https://wordstyle-backend.onrender.com | FastAPI 服务，数据库操作 |
| **数据库** | Supabase | PostgreSQL | 用户数据、订单、任务存储 |

### 4.2 环境变量配置

#### 4.2.1 前端环境变量（Streamlit Cloud）

**配置文件**：`.streamlit/secrets.toml`

```toml
# 后端 API 地址
BACKEND_URL = "https://wordstyle-backend.onrender.com"

# 启用 Supabase 模式
USE_SUPABASE = true

# 数据库连接（API 模式下不需要，但保留用于切换模式）
DATABASE_URL = "postgresql://..."
```

**必需变量**：
- `BACKEND_URL`：后端 API 地址
- `USE_SUPABASE`：启用数据库模式（true/false）

#### 4.2.2 后端环境变量（Render）

**配置方式**：Render 控制台 → Settings → Environment Variables

| 变量 | 必需 | 说明 | 示例值 |
|------|------|------|--------|
| `DATABASE_URL` | ✅ | Supabase连接池器地址 | `postgresql://postgres.{project_id}:密码@aws-{region}.pooler.supabase.com:6543/postgres` |
| `SECRET_KEY` | ✅ | JWT密钥 | `your-secret-key-here` |
| `ALLOWED_ORIGINS` | ✅ | CORS允许的源 | `https://*.streamlit.app` |
| `DEBUG` | ✅ | 调试模式 | `false` |
| `UPLOAD_DIR` | ✅ | 上传目录 | `/tmp/uploads` |

**重要说明**:
- DATABASE_URL必须使用Supabase提供的**连接池器地址**,而非直连地址
- 用户名格式为`postgres.{project_id}`(带项目ID)
- 端口为**6543**(连接池器),不是5432(直连)
- 主机名格式为`aws-{region}.pooler.supabase.com`(如`aws-1-ap-south-1.pooler.supabase.com`)
- 密码中的特殊字符需要URL编码(如`@`编码为`%40`)

### 4.3 自动化部署

#### 4.3.1 Render 自动部署

**配置文件**:`backend/render.yaml`

```yaml
services:
  - type: web
    name: wordstyle-api
    env: python
    region: singapore
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: "https://*.streamlit.app"
      - key: DEBUG
        value: "false"
      - key: UPLOAD_DIR
        value: "/tmp/uploads"
```

**关键配置说明**:
- `rootDir: backend`:指定后端代码位于backend子目录
- `startCommand`:启动时先执行`alembic upgrade head`自动迁移数据库,再启动Uvicorn服务
- `healthCheckPath: /health`:健康检查端点,用于Render监控服务状态
- `envVars`:环境变量配置,DATABASE_URL需在Render控制台手动设置

**部署流程**：
1. 推送代码到 GitHub
2. Render 自动检测变更
3. 执行 `buildCommand` 安装依赖
4. 执行 `startCommand` 启动服务
5. 服务上线

#### 4.3.2 Streamlit Cloud 部署

**部署流程**：
1. 推送代码到 GitHub
2. Streamlit Cloud 自动检测变更
3. 重新部署应用
4. 服务上线

**注意**：
- Streamlit Cloud 需要根目录的 `requirements.txt` 包含所有依赖
- 管理后台导入后端模块，因此必须包含 `sqlalchemy`、`psycopg2-binary` 等依赖

#### 4.3.3 Render部署问题排查

**常见问题及解决方案**：

1. **Alembic迁移失败 - ModuleNotFoundError: No module named 'app'**
   - **原因**：Python找不到app模块,因为alembic/env.py未正确设置sys.path
   - **解决**：在`backend/alembic/env.py`文件开头添加:
     ```python
     import sys
     import os
     sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
     ```

2. **数据库连接失败 - could not translate host name**
   - **原因**：使用了错误的连接池器地址格式或用户名格式
   - **解决**：
     - 使用Supabase提供的标准连接池器地址:`aws-{region}.pooler.supabase.com:6543`
     - 用户名格式必须为:`postgres.{project_id}`
     - 示例:`postgresql://postgres.cgfdhubkklpyvjgezeeq:密码@aws-1-ap-south-1.pooler.supabase.com:6543/postgres`

3. **URL编码错误 - invalid interpolation syntax**
   - **原因**：DATABASE_URL中的`%`(如`%40`)被Alembic的configparser误解析为插值语法
   - **解决**：在`backend/alembic/env.py`中转义百分号:
     ```python
     escaped_url = database_url.replace('%', '%%')
     config.set_main_option("sqlalchemy.url", escaped_url)
     ```

4. **IPv6连接失败 - Network is unreachable**
   - **原因**：Render尝试通过IPv6连接Supabase直连地址失败
   - **解决**：改用连接池器地址(端口6543),而非直连地址(端口5432)

5. **启动命令未执行Alembic迁移**
   - **原因**：Render缓存了旧的render.yaml配置
   - **解决**：在Render控制台手动修改Start Command为:`alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT`

### 4.4 数据流

#### 4.4.1 用户首次访问流程

```
1. 用户打开 Streamlit Cloud 应用
   ↓
2. app.py 初始化
   - 检查 session_state 中是否有 user_id
   - 如无，生成新的 12 位 user_id（基于 hostname 哈希）
   ↓
3. 调用 data_manager.load_user_data(user_id)
   - 在 API 模式下，调用后端 API: GET /api/users/{user_id}
   ↓
4. 调用 claim_free_paragraphs(user_id)
   - 检查是否是今日首次访问
   - 如是，赠送 10,000 免费段落
   ↓
5. 调用 register_or_login_user(user_id, user_data)
   - 在 API 模式下，调用后端 API: POST /api/users/{user_id}
   - 后端执行 SQL: INSERT INTO users (...)
   ↓
6. 用户数据写入 Supabase 数据库
   - id: 12位字符串
   - balance: 0.0
   - paragraphs_remaining: 10000
   - total_converted: 0
   - is_active: True
   - created_at: NOW()
   - last_login: NOW()
```

#### 4.4.2 文档转换流程

```
1. 用户上传文档并点击"开始转换"
   ↓
2. 前端调用后端 API 创建任务
   - POST /api/tasks
   - 请求体: {user_id, filename, paragraphs, cost}
   ↓
3. 后端创建任务记录
   - INSERT INTO conversion_tasks (...)
   - 返回 task_id
   ↓
4. 前端轮询任务状态
   - GET /api/tasks/{task_id}
   - 每 2 秒查询一次
   ↓
5. 后端执行转换（异步）
   - 更新任务状态: PENDING → PROCESSING → COMPLETED
   - 更新进度: 0 → 100
   ↓
6. 转换完成
   - 更新用户数据: paragraphs_remaining -= paragraphs
   - 更新用户统计: total_converted += 1
   ↓
7. 前端显示下载链接
```

---

## 5. 安全设计

### 5.1 用户隐私保护

- ❌ 不收集个人敏感信息（姓名、手机号、邮箱等）
- ❌ 不上传文档内容到第三方服务器
- ✅ 仅统计转换次数和使用量
- ✅ 用户ID为匿名哈希字符串

### 5.2 数据安全

- 数据库连接使用 SSL 加密
- API 通信使用 HTTPS
- 敏感配置存储在环境变量中
- 定期备份数据库

### 5.3 输入验证

- 文件格式验证（仅允许 .docx）
- 文件大小限制（最大 50MB）
- SQL 注入防护（使用 ORM）
- XSS 防护（Streamlit 自动转义）

### 5.4 临时文件管理

- 转换完成后立即清理临时文件
- 启动时清理过期临时文件（>7天）
- 异常情况下确保文件清理

---

## 6. 性能优化

### 6.1 前端优化

- 使用 `st.cache_data` 缓存样式映射配置
- 避免不必要的 `st.rerun()` 调用
- 大文件上传显示进度条
- 长时间操作提供进度反馈

### 6.2 后端优化

- 数据库连接池管理
- API 响应缓存
- 异步任务处理（待实现）
- 数据库索引优化

### 6.3 转换引擎优化

#### 6.3.1 样式映射缓存
- 使用 `st.session_state` 缓存源文件样式列表
- 避免重复分析同一文件的样式结构

#### 6.3.2 批量处理段落
- 一次性遍历完成段落计数和样式分析
- 减少文件I/O操作次数

#### 6.3.3 ⚡ 一次性流水线优化（2026-04-30）

**问题背景**：
原有的`full_convert()`方法采用三阶段流水线架构，每个阶段独立加载、解析、保存文档，导致大量时间浪费在重复的Document加载和文件I/O操作上。

**优化方案**：
将三个阶段合并为**一次性流水线**，在内存中完成所有转换，只进行一次加载和一次保存。

```python
# 优化前：
Load → StyleConv → Save → Load → MoodConv → Save → Load → AnswerInsert → Save

# 优化后：
Load → StyleConv → MoodConv → AnswerInsert → Save
```

**实施细节**：
1. 重构`full_convert()`方法，调用三个新的内存处理方法
2. 新增`_convert_styles_in_memory()`：在内存中进行样式转换
3. 新增`_convert_mood_in_memory()`：在内存中进行语气转换
4. 新增`_insert_response_in_memory()`：在内存中插入应答句
5. 保留原有方法，确保向后兼容

**性能提升**：
- Document加载次数：3次 → 1次（减少67%）
- 文件保存次数：3次 → 1次（减少67%）
- 临时文件数量：2个 → 0个（减少100%）
- 实际测试：0.22秒 → 0.09秒（提升57.3%）
- 大型文档预估：36-80秒 → 15-30秒（节省21-50秒）

**代码位置**：
- `doc_converter.py:1892-1986` - `full_convert()`方法重构
- `doc_converter.py:2054-2116` - `_convert_styles_in_memory()`方法
- `doc_converter.py:2118-2147` - `_convert_mood_in_memory()`方法
- `doc_converter.py:2149-2216` - `_insert_response_in_memory()`方法

**测试验证**：
- 测试脚本：`test_performance_optimization.py`
- 测试报告：`PERFORMANCE_TEST_REPORT.md`
- 输出一致性：✅ 验证通过（差异仅为一个末尾空段落，不影响功能）

**详见**：[03-Bug修复记录文档.md](./03-Bug修复记录文档.md) - 优化 #001

#### 6.3.4 内存管理
- 及时释放大对象（Document对象、临时文件）
- 避免内存泄漏

#### 6.3.5 并行处理（待实现）
- 考虑使用多线程/多进程处理多个文档
- 注意GIL限制和线程安全问题

---

## 7. 监控与日志

### 7.1 日志记录

**前端日志**：
- 用户操作日志
- 转换任务日志
- 错误日志

**后端日志**：
- API 请求日志
- 数据库操作日志
- 错误堆栈日志

### 7.2 监控指标

- 活跃用户数
- 转换任务成功率
- API 响应时间
- 数据库连接数

### 7.3 告警机制

- 服务宕机告警
- 数据库连接失败告警
- 转换失败率过高告警

---

## 8. 维护指南

### 8.1 日常维护

- 定期检查数据库存储空间
- 清理过期临时文件
- 监控系统资源使用情况
- 查看错误日志

### 8.2 故障排查

**常见问题**：

1. **前端无法连接后端**
   - 检查 `BACKEND_URL` 配置
   - 访问 `/api/health` 确认后端运行
   - 查看 Render 控制台日志

2. **用户数据无法写入**
   - 检查 `users` 表 `id` 字段类型是否为 `VARCHAR(12)`
   - 检查后端日志中的 SQL 错误
   - 运行测试脚本验证

3. **转换失败**
   - 查看任务错误信息
   - 检查文档格式是否正确
   - 查看后端日志中的详细错误

### 8.3 数据库迁移

**使用 Alembic 进行迁移**：

```bash
cd backend

# 创建新迁移
alembic revision -m "description"

# 执行迁移
alembic upgrade head

# 回滚迁移
alembic downgrade -1
```

**迁移文件位置**：`backend/alembic/versions/`

---

## 4. 文件清理系统设计

### 4.1 需求背景

在文档转换过程中，系统会产生三类文件：
1. **临时源文件**（temp_source_*）：用户上传的源文档副本
2. **临时模板文件**（temp_template_*）：用户上传的模板文档副本
3. **转换结果文件**（result_*.docx）：转换完成的文档

为避免服务器存储空间无限增长，需要实现自动清理机制。

### 4.2 设计原则

#### 4.2.1 文件保留策略

| 文件类型 | 保留时间 | 清理时机 |
|---------|---------|----------|
| temp_source_* | 不保留 | 转换完成后立即清理 |
| temp_template_* | 不保留 | 转换完成后立即清理 |
| result_*.docx | 7天 | 服务启动时 + 每天零点自动清理 |

#### 4.2.2 清理触发机制

系统支持三种清理触发方式：

1. **服务启动时清理**：每次应用启动时执行一次全面清理
2. **转换完成后清理**：每次转换完成后清理本次产生的临时文件
3. **定时任务清理**：每天零点自动清理所有过期文件

### 4.3 架构设计

#### 4.3.1 模块划分

```
file_manager.py (核心逻辑层)
    ↓
data_manager.py (数据访问层 - 统一接口)
    ↓
backend/app/api/admin.py (API路由层 - FastAPI)
    ↓
admin_web.py (前端展示层 - Streamlit)
```

#### 4.3.2 类与函数设计

**FileManager 类** (`file_manager.py`)

```python
class FileManager:
    """文件管理器 - 处理文件清理和查询"""
    
    def __init__(self, base_dir: str = ".", results_dir: str = "conversion_results"):
        """初始化文件管理器"""
        
    def cleanup_temp_files(self, user_id: str = None) -> Dict[str, int]:
        """清理临时文件（源文件和模板文件）"""
        
    def cleanup_expired_results(self, retention_days: int = 7) -> Dict[str, int]:
        """清理过期的转换结果文件"""
        
    def get_file_list(self, page: int = 1, page_size: int = 50) -> Dict:
        """获取文件列表（支持分页）"""
        
    def delete_files(self, file_ids: List[str]) -> Dict[str, int]:
        """删除指定的文件"""
        
    def cleanup_all_expired(self) -> Dict[str, int]:
        """清理所有过期文件"""
        
    def get_storage_stats(self) -> Dict:
        """获取存储空间统计信息"""
```

**全局函数**

```python
def cleanup_on_startup():
    """服务启动时清理过期文件"""
    
def schedule_daily_cleanup():
    """每日定时清理任务（由APScheduler调用）"""
```

### 4.4 集成方案

#### 4.4.1 app.py 集成

**A. 服务启动时清理**

位置：app.py 第36-41行

```python
# ✅ 服务启动时执行文件清理
try:
    from file_manager import cleanup_on_startup
    cleanup_on_startup()
except Exception as e:
    logger.warning(f"启动时文件清理失败（不影响服务）: {e}")
```

**B. 每日定时清理任务**

位置：app.py 第43-63行

```python
# ✅ 设置每日定时清理任务（每天零点执行）
try:
    from apscheduler.schedulers.background import BackgroundScheduler
    from file_manager import schedule_daily_cleanup
    
    scheduler = BackgroundScheduler()
    scheduler.add_job(
        func=schedule_daily_cleanup,
        trigger='cron',
        hour=0,
        minute=0,
        id='daily_file_cleanup',
        name='每日文件清理任务'
    )
    scheduler.start()
    logger.info("✅ 每日文件清理任务已启动（每天零点执行）")
except ImportError:
    logger.warning("⚠️ APScheduler未安装，跳过定时清理任务")
except Exception as e:
    logger.warning(f"定时清理任务启动失败: {e}")
```

**C. 转换完成后清理临时文件**

位置：app.py 第1606-1613行

```python
# ✅ 清理临时文件（源文件和模板文件）
try:
    from file_manager import get_file_manager
    fm = get_file_manager()
    cleanup_stats = fm.cleanup_temp_files(st.session_state.user_id)
    logger.info(f"临时文件清理完成: {cleanup_stats}")
except Exception as cleanup_error:
    logger.warning(f"临时文件清理失败（不影响转换结果）: {cleanup_error}")
```

**D. 输出文件路径修改**

位置：app.py 第1456-1460行

```python
# 输出文件路径 - 保存到conversion_results目录
base_name = os.path.splitext(source_file_obj.name)[0]
output_filename = f"result_{base_name}_{datetime.now().strftime('%Y%m%d_%H%M%S')}.docx"
output_file = os.path.join("conversion_results", output_filename)
```

#### 4.4.2 data_manager.py 统一API

位置：data_manager.py 第1100-1182行

提供三个统一的文件管理API函数，支持local和api双模式：

```python
def get_file_list(page: int = 1, page_size: int = 50) -> Dict:
    """获取文件列表（支持分页）"""
    if DATA_SOURCE == "api":
        # 调用后端API
        api_url = f"{BACKEND_URL.rstrip('/')}/admin/files?page={page}&page_size={page_size}"
        response = requests.get(api_url, timeout=10)
        return response.json()
    elif DATA_SOURCE == "local":
        # 本地模式使用file_manager
        from file_manager import get_file_manager
        fm = get_file_manager()
        return fm.get_file_list(page=page, page_size=page_size)

def delete_files(file_ids: List[str]) -> Dict[str, int]:
    """删除指定的文件"""
    # 类似的双模式实现

def get_storage_stats() -> Dict:
    """获取存储空间统计信息"""
    # 类似的双模式实现
```

#### 4.4.3 后端FastAPI路由

位置：backend/app/api/admin.py 第524-594行

```python
@router.get("/files")
def get_files_list(page: int = 1, page_size: int = 50):
    """获取文件列表（支持分页）"""
    
@router.post("/files/delete")
def delete_files_endpoint(file_ids: list = Body(...)):
    """删除指定的文件"""
    
@router.get("/files/stats")
def get_storage_statistics():
    """获取存储空间统计信息"""
```

#### 4.4.4 admin_web.py 文件管理页面

位置：admin_web.py 第671-856行

**页面功能**：

1. **存储统计卡片**（4个指标）
   - 📄 临时源文件数量和大小
   - 📋 临时模板文件数量和大小
   - ✅ 转换结果文件数量和大小
   - ⏰ 过期文件数量

2. **文件列表表格**
   - 列：选择、用户ID、文件名、文件类型、生成时间、大小(KB)、状态
   - 每页显示20条记录
   - 支持分页导航

3. **多选功能**
   - 单个checkbox选择
   - 全选/取消全选按钮
   - 选中文件ID保存在session_state中

4. **批量操作**
   - 删除选中文件（带确认对话框）
   - 清理所有过期文件按钮

### 4.5 依赖管理

**requirements.txt 新增依赖**：

```txt
# 定时任务
APScheduler>=3.10.0
```

### 4.6 错误处理策略

#### 4.6.1 清理失败不影响主流程

所有清理操作都包裹在try-except中，失败时只记录警告日志，不抛出异常：

```python
try:
    cleanup_stats = fm.cleanup_temp_files(st.session_state.user_id)
    logger.info(f"临时文件清理完成: {cleanup_stats}")
except Exception as cleanup_error:
    logger.warning(f"临时文件清理失败（不影响转换结果）: {cleanup_error}")
```

#### 4.6.2 单个文件失败不影响其他文件

批量删除时，即使某个文件删除失败，也会继续处理其他文件：

```python
for file_id in file_ids:
    try:
        file_path.unlink()
        stats['success'] += 1
    except Exception as e:
        stats['failed'] += 1
        stats['errors'].append(f"删除失败 {file_id}: {str(e)}")
```

#### 4.6.3 APScheduler未安装时的优雅降级

```python
except ImportError:
    logger.warning("⚠️ APScheduler未安装，跳过定时清理任务")
```

### 4.7 性能优化

#### 4.7.1 分页加载

文件列表采用分页机制，避免一次性加载大量数据：

- 默认每页20条记录
- 支持页码跳转
- 减少内存占用和渲染时间

#### 4.7.2 按需清理

- 转换完成后只清理当前用户的临时文件
- 定时任务清理所有过期文件
- 避免不必要的文件系统扫描

### 4.8 安全性考虑

#### 4.8.1 文件路径验证

FileManager通过解析file_id来确定文件路径，防止路径遍历攻击：

```python
if file_id.startswith('source_'):
    filename = file_id.replace('source_', '', 1)
    file_path = self.base_dir / filename
elif file_id.startswith('template_'):
    filename = file_id.replace('template_', '', 1)
    file_path = self.base_dir / filename
elif file_id.startswith('result_'):
    filename = file_id.replace('result_', '', 1)
    file_path = self.results_dir / filename
```

#### 4.8.2 删除确认

批量删除前显示确认对话框，防止误操作：

```python
st.warning(f"⚠️ 确定要删除 {len(selected_files)} 个文件吗？此操作不可恢复！")
if st.button("✅ 确认删除", type="primary"):
    # 执行删除
```

### 4.9 监控与日志

#### 4.9.1 关键日志点

1. **启动清理**：
   ```python
   logger.info(f"启动清理完成: {stats}")
   ```

2. **定时任务**：
   ```python
   logger.info("✅ 每日文件清理任务已启动（每天零点执行）")
   logger.info(f"每日清理完成: {stats}")
   ```

3. **转换后清理**：
   ```python
   logger.info(f"临时文件清理完成: {cleanup_stats}")
   ```

4. **手动清理**：
   ```python
   logger.info(f"已删除文件: {file_path.name}")
   ```

#### 4.9.2 错误日志

```python
logger.error(f"删除文件失败 {file_id}: {e}")
logger.warning(f"启动时文件清理失败（不影响服务）: {e}")
```

### 4.10 测试要点

详见 `docs/文件管理功能测试指南.md`

主要测试场景：
1. 服务启动时自动清理
2. 转换完成后自动清理临时文件
3. 每日定时清理任务
4. 文件管理页面功能（统计、列表、多选、删除、分页）
5. API模式与Local模式一致性

### 4.11 部署注意事项

#### 4.11.1 Render平台

- 确保conversion_results目录存在且有写权限
- APScheduler在Render上可能受限制，建议使用Render的Scheduled Jobs功能替代
- 定期监控存储空间使用情况

#### 4.11.2 Streamlit Cloud

- Streamlit Cloud可能不支持后台线程，APScheduler可能无法正常工作
- 建议只在Local模式或自有服务器上使用定时清理功能
- 可以依赖服务重启时的清理机制

---

### 4.12 转换结果警告信息展示改进

#### 4.12.1 改进背景

之前的实现使用`st.expander`折叠展示警告明细，但用户反馈：
- 折叠方式对用户意义不大
- 明细信息过于繁琐
- 需要更简洁的汇总信息

#### 4.12.2 新的实现方式

**汇总显示逻辑**（app.py 第1399-1417行）：

```python
# 统计合并单元格表格数
merge_table_count = sum(1 for w in result['warnings'] if '合并单元格' in w)
# 统计OLE/VML对象数
ole_vml_count = sum(1 for w in result['warnings'] if 'OLE/VML 对象' in w or 'OLE' in w or 'VML' in w)

# 显示汇总警告信息
if merge_table_count > 0 or ole_vml_count > 0:
    warning_parts = []
    if merge_table_count > 0:
        warning_parts.append(f"{merge_table_count}个有合并单元格的表格")
    if ole_vml_count > 0:
        warning_parts.append(f"{ole_vml_count}个OLE对象图片")
    
    warning_summary = "，".join(warning_parts)
    st.warning(
        f"⚠️ **{result['name']}** 文件中包含{warning_summary}，请手动处理。"
    )
```

#### 4.12.3 警告信息生成修复

**Bug修复**（doc_converter.py）：

1. **`copy_special_element`函数**（第439-468行）：
   - 添加`warning_callback`参数
   - 在检测到OLE/VML对象时生成警告
   - 警告格式：`[WARNING] 检测到 X 个 OLE 对象和 Y 个 VML 形状，请手动检查转换结果`

2. **调用处修改**（第946行）：
   - 传入`warning_callback`参数
   - 确保特殊元素（非段落/非表格）的OLE/VML对象也能生成警告

#### 4.12.4 完整的警告检测路径

| 检测路径 | 处理函数 | warning_callback | 状态 |
|---------|---------|-----------------|------|
| 普通段落含OLE/VML | `copy_paragraph_with_images` | ✅ 传递 | ✅ 正常 |
| 表格含合并单元格 | `copy_table_with_images` | ✅ 传递 | ✅ 正常 |
| 表格单元格含OLE/VML | `copy_table_with_images` | ✅ 传递 | ✅ 正常 |
| 特殊元素（OLE/VML） | `copy_special_element` | ✅ 传递 **[已修复]** | ✅ 正常 |

#### 4.12.5 UI展示示例

**修改前**：
```
✅ 8js3.docx 转换成功
  ⚠️ 8js3.docx 的警告信息: (可展开)
    - 表格 35 包含合并单元格...
    - 表格 41 包含合并单元格...
    ...
```

**修改后**：
```
✅ 8js3.docx 转换成功
⚠️ **8js3.docx** 文件中包含19个有合并单元格的表格，69个OLE对象图片，请手动处理。
```

#### 4.12.6 优势

1. **信息简洁**：一目了然，不需要展开/折叠
2. **关键信息突出**：直接显示统计数字
3. **用户体验好**：减少交互步骤
4. **符合编码原则**：不破坏已有功能，系统性思考

---

**文档结束**
