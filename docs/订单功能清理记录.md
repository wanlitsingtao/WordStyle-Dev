# 订单功能清理记录

## 📅 清理日期
2026-04-30

## 🎯 清理原因
当前版本没有实现订单管理功能，为简化代码和数据库结构，决定移除所有订单相关代码和数据表。

## 🗑️ 已删除的文件

### 后端文件
1. `backend/app/api/payments.py` - 支付API路由（完整删除）
2. `backend/app/api/test_payment.py` - 测试支付API（完整删除）

## ✏️ 已修改的文件

### 后端代码
1. **backend/app/models.py**
   - 删除 `Order` 模型类（第56-71行）

2. **backend/app/schemas.py**
   - 删除 `OrderResponse` 类（第75-90行）

3. **backend/app/main.py**
   - 移除 `payments` 模块导入
   - 移除 `test_payment` 模块导入
   - 移除 `/api/payments` 路由注册
   - 移除 `/api/test-payment` 路由注册

4. **backend/test_api.py**
   - 删除 `test_create_order()` 函数
   - 移除 main() 函数中对 `test_create_order()` 的调用

### 文档更新
1. **docs/02-系统设计文档.md**
   - 删除 3.2.2 orders 表完整定义（约43行）
   - 重新编号后续章节：3.2.3 → 3.2.2, 3.2.4 → 3.2.3, 等
   - 删除实体关系图中的 orders 表
   - 删除 API 变更说明中的 `/api/orders` 端点
   - 删除 backend 目录结构中的 `orders.py`

2. **docs/05-系统部署详细文档.md**
   - 删除架构图中的 orders 表
   - 删除数据库表验证列表中的 orders 表

3. **docs/01-业务需求文档.md**
   - 删除"后续升级方向"中的"订单管理"
   - 删除 Phase 3 功能列表中的"订单管理系统"

## 💾 数据库变更

### 需要执行的SQL（云端 Supabase）
```sql
-- 删除 orders 表
DROP TABLE IF EXISTS orders CASCADE;
```

**注意**：由于使用 `CASCADE`，会自动删除依赖该表的外键约束。

### 本地 SQLite
如果存在本地数据库文件，执行：
```bash
python clean_orders.py
```

## ✅ 保留的功能

虽然删除了订单管理，但以下充值相关功能仍然保留：
- ✅ 用户余额管理（`users.balance`）
- ✅ 段落数管理（`users.paragraphs_remaining`）
- ✅ 充值历史记录（在 `user_manager.py` 中通过 JSON 存储）
- ✅ 手动充值功能（管理员后台）

## 🔄 影响范围

### 不受影响的功能
- ✅ 用户注册/登录
- ✅ 文档转换功能
- ✅ 任务管理
- ✅ 评论系统
- ✅ 反馈系统
- ✅ 样式映射
- ✅ 管理员后台

### 受影响的功能
- ❌ 自动支付流程（暂未实现，无影响）
- ❌ 订单查询（暂未实现，无影响）
- ❌ 支付回调处理（暂未实现，无影响）

## 📝 后续建议

如果未来需要实现订单功能，建议：
1. 重新设计订单表结构，考虑实际需求
2. 集成真实的支付网关（微信支付、支付宝）
3. 实现完整的订单生命周期管理
4. 添加订单状态机
5. 实现支付回调和自动充值

## 🔍 验证清单

- [x] 后端代码编译无错误
- [x] 删除所有 Order 相关模型
- [x] 删除所有 Order 相关 API 端点
- [x] 更新所有相关文档
- [ ] 在云端 Supabase 执行 DROP TABLE orders
- [ ] 验证前端应用正常运行
- [ ] 验证管理员后台正常运行
- [ ] 验证用户充值功能正常（手动充值）

---

**清理执行人**: AI Assistant  
**审核人**: 待定  
**状态**: 代码清理完成，待云端数据库执行
